# Slack Connector Profile

Slack is registered with `managed_bridge` and `webhook` transports. Its module owns the unchanged setup schemas, Events API normalizer, Slack-compatible signature bases and headers, acknowledgements, outbound mapping and retry metadata, configuration verification, and official references.

The baseline profile has no live test and does not implement `official_api`. HTTP execution remains in the provider-neutral adapter.
