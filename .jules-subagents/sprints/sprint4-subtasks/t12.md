title: Add `ActiveExecutionCoordinator` (Foundation-First Lock)
depends_on: ["t11"]
is_independent: true
merged: false
prompt:
# Task Specification: T12 - 4

## Objective
A coordinator lock enables safe rollout while preserving future extensibility.
Add process-level execution lock service enforcing one active `start_sprint`/`start_subtask` pipeline at a time.

## Technical Details & Research Findings
- **Context**: Sprint 4 intentionally does not ship full concurrent multi-project execution.
- **Done Criteria**: Concurrent start attempts return explicit `ACTIVE_RUN_EXISTS` response with run metadata.

## Execution Steps
1. Follow the instructions to implement this task.
2. Verify against the done criteria.

## Engineering Standard
- Use the feature branch: feature/sprint4-implementation
- Ensure all tests pass before completing.
