title: Implement Telemetry Capture for Tokens, Duration, and Provider Outcomes
depends_on: ["t16", "t20"]
is_independent: true
merged: false
prompt:
# Task Specification: T28 - 4

## Objective
Telemetry must be captured at run/task boundaries during execution, not reconstructed later.
Record usage samples per provider run with token fields, duration, completion state, and failure category.

## Technical Details & Research Findings
- **Context**: Token and duration analytics are not persisted as first-class entities.
- **Done Criteria**: Every task run writes duration + provider + token metrics (or explicit `unknown` markers).

## Execution Steps
1. Follow the instructions to implement this task.
2. Verify against the done criteria.

## Engineering Standard
- Use the feature branch: feature/sprint4-implementation
- Ensure all tests pass before completing.
