title: Refactor Orchestrator Args to Project/Sprint Identity
depends_on: ["t11"]
is_independent: true
merged: false
prompt:
# Task Specification: T13 - 4

## Objective
DB-backed orchestration should be identity-first.
Extend orchestration argument contracts to include `project_id` and `sprint_id`, with compatibility mapping for legacy args.

## Technical Details & Research Findings
- **Context**: Orchestrator inputs are currently `sprint_number` and `repo_path`.
- **Done Criteria**: New tools use identity-first args; legacy paths route via compatibility resolver.

## Execution Steps
1. Follow the instructions to implement this task.
2. Verify against the done criteria.

## Engineering Standard
- Use the feature branch: feature/sprint4-implementation
- Ensure all tests pass before completing.
